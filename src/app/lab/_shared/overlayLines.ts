// Figma node 7438:44091, frames 1-3. Note this is NOT how either hero breaks
// the sentence - the vertical Hero reads ".../ flying blind on every model /
// decision." and the deck's HeroSlide wraps differently again at its own width.
// The word SEQUENCE is identical in all three, which is the only thing index
// pairing depends on; the differing breaks are what make words visibly migrate
// between lines during the snap.
//
// Shared by both lab routes so the two entrances stay the same composition -
// it's the same case study arriving, only the destination differs.
export const OVERLAY_LINES = [
  "Enterprise AI teams were",
  "flying blind on every",
  "model decision.",
];
