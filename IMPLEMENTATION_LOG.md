# Implementation Log — Portfolio

*Deviations from spec only. Format: Date — what changed — why.*

- **2026-07-11** — Hero.tsx letter-spacing corrected to 0.78px/0.085px (was 6px/0.5px). Previous values were measured wrong against Figma's label/body-semibold styles.
- **2026-07-11** — ProductBrowserMockup.tsx switched to ScreenshotFrame's shared gradient/border-frame/radius-frame treatment instead of its own flat background, once ScreenshotFrame's own treatment was reconciled against Figma.
- **2026-07-11** — DecisionAssetFrame.tsx: corrected capsule border/fade geometry (no bottom fade, asymmetric rounding) and found the ring border is a gradient, not the flat #D5D5D5 the simplified Tailwind conversion assumed — Figma's raw layer tree disagreed with the actual rendered pixels on direct sampling.
- **2026-07-11** — ScreenshotFrame.tsx built with a solid white (bg-surface) fill rather than matching one specific Figma instance that samples as flat #F5F5F5 with an invisible border. Followed the documented system spec over that one instance's sampled values.
- **2026-07-12** — Mobile responsive fallbacks added across CockpitPlaceholder.tsx, CouncilIntro.tsx, MetaBar.tsx, and Hero.tsx (dropped nowrap/fixed line-breaks, 2x2 grid, hidden dividers/connectors below `md`) — no mobile Figma frame exists for any of these, so proportions/breakpoints are engineering judgment.
- **2026-07-12** — FiveDecisions.tsx: CSS Grid layout (spanning the asset across title/body rows) replaced with a flexbox `contents`+`order` technique. Grid's row-track sizing inflated rows to fit tall assets, producing an asset-height-dependent gap under the title.
- **2026-07-12** — HonestFailure.tsx: added a `mb-60px` margin not present in Figma, since the bg-light callout box sat flush against the section below and read as one continuous block instead of a self-contained callout.
- **2026-07-12** — DecisionAssetFrame.tsx rebuilt from a corrected, explicitly-annotated Figma reference (gradient endpoint changed #FFFFFF → bg-primary) once one was supplied, and converted to a fluid aspect-ratio box on mobile since no mobile frame exists for it. ScreenshotFrame.tsx made the same fixed-height → aspect-ratio change for the same reason.
- **2026-07-13** — Reflections.tsx cards re-synced to a Figma redesign (card-within-card replacing the old flat bordered card), with some copy/line-break behavior revised to match — the person redesigned the cards and copy in Figma after the component was first built.
- **2026-07-14** — Display typeface swapped Fraunces → Season Mix (self-hosted, static single-weight Heavy/900), per direct request. Cascaded into: all `fontVariationSettings` SOFT/WONK axis code removed (17 call sites — Season Mix has no fvar/gvar/STAT table); `font-semibold` next to `font-display` now renders at the same 900 weight regardless (a static font can't be thinned); and the registered weight corrected from an earlier assumed Bold/700 to the real Heavy/900 after inspecting the file.
- **2026-07-14** — FastRouterLogomark.tsx: logomark SVG inlined as JSX instead of loaded as an external file, since externally-loaded SVGs can't inherit host-page CSS vars/currentColor/dark-mode classes — the logo was invisible in dark mode otherwise.
- **2026-07-14** — globals.css dark-mode bg-primary/bg-surface/bg-light replaced a flat, visibly blue-tinted Figma-bound value (#1A1A2E) with a neutral scale, then re-tuned the same day to an evenly-stepped scale anchored on #1A1A1A — flagged as blue-tinted with no surface separation, then found the reference site's actual background was darker than the first pass.
- **2026-07-15** — ProductBrowserMockup.tsx browser-window hairline changed from a hardcoded light-mode hex (#E9E9E9) to the border-default token — the hardcoded value stayed frozen at light-mode once dark mode shipped, reading as a bright ring around the dark screenshot.
- **2026-07-16** — RejectedVsShipped.tsx dark-mode images added by appending "-dark" to whichever (already-swapped) filename is actually referenced at each call site, keeping dark assets consistent with a pre-existing swapped-filename workaround rather than compounding a second one.
- **2026-07-16** — Per-block widths across EvaluationsFeature.tsx, ObservabilityIntro.tsx, and RejectedVsShipped.tsx (1016/1136/1200/1280/1160px) unified into one shared 1060px section / 980px content width; globals.css's whole content max-width system collapsed the same way. Per direct instruction to standardize the page on one width after the old per-block widths caused real overflow.
- **2026-07-16** — Footer.tsx (fastrouter) widened from the 1060px content width to max-w-[1440px] and moved to a sibling of the rail/main/placeholder row rather than nested inside it, per direct feedback that it needed to span the full page composition.
- **2026-07-16** — fastrouter/page.tsx added a page-level 1440px framing wrapper (rail | 1060px main | placeholder), rail fixed at 190px — per direct request, matching a reference site's layout rather than anything in FastRouter's Figma file.
- **2026-07-16** — SectionRail.tsx: visibility toggle switched from edge-triggered onEnter/onLeaveBack to an onUpdate handler re-deriving visibility from live scroll position, since the edge-triggered version could get stuck out of sync if the effect re-ran mid-scroll. Same file also repositioned (pinned below header instead of vertically centered, per direct feedback), switched to per-item staggered animation (per direct request), and moved from `position: fixed` to `sticky` with its visibility breakpoint moved 768px → 1300px (an unmeasured estimate).
- **2026-07-17** — CouncilIntro.tsx: removed the "4 weeks, brief to shipping" / "No existing UX pattern" badges under the headline, per direct feedback, for more breathing room.
- **2026-07-17** — EvaluationsFeature.tsx: removed the "REJECTED vs shipped" eyebrow and "Two attempts. Same flaw." line, folding the idea into the title paragraph instead, per direct feedback to avoid repeating the same idea twice. Same file plus ObservabilityIntro.tsx also had an earlier left-indent (pl-40px/pl-90px) reversed, per feedback that it misaligned the label against the title.
- **2026-07-17** — FiveDecisions.tsx on-page eyebrow corrected "FIVE DECISIONS" (Figma's stale text) → "FOUR DECISIONS", since only 4 of 5 Figma-authored decisions are actually visible (the 5th is hidden in Figma).
- **2026-07-18** — ShareButton.tsx, AssetPlaceholder.tsx, and DecisionAssetFrame.tsx promoted from fastrouter/ to shared/ — Analytics and Extensions needed the identical placeholder/frame/share behavior with no FastRouter-specific content in any of them.
- **2026-07-18** — All three case studies' ReadNext.tsx: "View case study →" teaser links, originally hidden in Figma while the other case studies had no pages, wired to real routes; the near-identical card markup extracted into a new shared ProjectCard.tsx. Also added a temporary `/` project index page (src/app/page.tsx) reusing ProjectCard, since every case study's SectionRail already pointed `backHref="/"` at a route with nothing built behind it.
- **2026-07-19** — Practice section (src/components/practice/) shipped phase-1 with only two build posts (Trace, Glyphflow), every copy field a literal `[PLACEHOLDER]` string and no live `visitUrl` — no real project info exists anywhere in the repo, and inventing plausible copy is against policy.
- **2026-07-19** — Asset naming convention changed from CLAUDE.md's `{prefix}-{number}-{feature}-{state}` to `{prefix}-{feature}-{state}` (no sequence number) for new Analytics/Extensions assets going forward, per direct instruction — assets aren't created in a fixed order, so maintaining a running per-feature number was an unnecessary maintenance burden.
- **2026-07-27 / 2026-07-28** — analytics/DecisionList.tsx's shared `Decision.asset` field changed from a string label (always rendered via AssetPlaceholder) to a `React.ReactNode`, across FeatureMetricCreation.tsx, FeatureQueryBreakdown.tsx, and FeatureCalendarCompare.tsx — real per-decision media started landing and the row template needed to render either a placeholder or a real ThemeAwareVideo/ThemeSwap asset without knowing which in advance.
- **2026-07-31** — CLAUDE.md: added "Positioning" and "Two build modes" sections, appended a person-directed-exception clause to "Build order," and fixed "Assets" to the no-number `{prefix}-{feature}-{state}` convention plus the mp4 (motion/perf)/webp (stills fallback)/png (legacy) format rationale.
- **2026-07-31** — All existing numbered FastRouter assets (`fr-01-...` through `fr-06-...`) retroactively renamed to the no-number convention decided 2026-07-19, per direct instruction — decision-index numbers embedded in feature names themselves (`decision-01` through `decision-04`) were left alone since they identify which of five decisions, not sequence cruft from the old convention. Every in-repo reference updated to match.
- **2026-07-31** — analytics/FeatureMetricTaxonomy.tsx decision-01 copy ("Six categories from scratch") corrected against the actual shipped screenshot once it landed — listed categories (Base, Calculated, Filtered, Transformed, Percentile, Faulty) didn't match what the asset shows (Base, Time, Transformed, Segment, Filter group, plus a separate "Faulty dimension" toggle rather than a sixth list item). Stale `alt` text with the same wrong category list, missed in the first copy fix, corrected in the same pass.
- **2026-08-03 / 2026-08-04** — extensions/FeatureMaps.tsx decision 01 ("ATF is the extension") wired with `object-left-top` (not the default center crop) — the recording's left sidebar sat at the frame's left edge and a center-anchored crop clipped through it.
- **2026-08-04** — extensions/GestureSeamVideo.tsx built (decision 02) — a one-off GSAP component panning/zooming a portrait (816×1286) recording's crop in sync with its own `currentTime`, since the chevron-tap interaction this decision is about sits near the very bottom of the frame, which a landscape `object-cover` crop never revealed at any point in the loop. Not promoted to shared/ — choreography specific to this one recording's content.
- **2026-08-04** — A taller capsule (`720/1100` vs. the standard `720/460`) was tried on decision 02 so the chevron would be visible at rest, then reverted same day per direct feedback — the pan/zoom's `reveal` phase was retimed earlier instead (see next entry), keeping the capsule consistent with sibling decisions.
- **2026-08-04** — GestureSeamVideo.tsx phase timing corrected twice against actual frame sampling, not assumption: the `settle` hold was first assumed to run to ~87% of the clip, but the recording actually reverses to the map view at ~60–62%; separately, the `reveal` phase's trigger moved 0.36 → 0.26 → 0.20 after discovering the light and dark recordings' tap moments land at different fractional points (~41% vs. ~37–38%) despite different absolute durations (29.18s vs. 21.32s) — a shared enterAt tuned only against light left dark with under 2s of hold time before the tap.
- **2026-08-05 / 2026-08-06** — extensions/FeatureMaps.tsx decision 03 ("Left panel always visible") and extensions/FeaturePhotoEditor.tsx decision 01 ("Separated traditional and AI tools") wired — the last two placeholders in the Extensions case study.
- **2026-08-06** — extensions/FeaturePhotoEditor.tsx crop changed from `object-top` (center) to `object-left-top` — full-timeline sampling showed the recording spends most of its length on an Image Tools dropdown (not the brief symmetric landing-page moment checked first), and that dropdown's logo/nav column sits at the left edge, which the center crop was cutting into.
- **2026-08-07** — analytics/FeatureCalendarCompare.tsx and FeatureMetricTaxonomy.tsx: the "pending this to analytics" kicker and the REJECTED/REJECTED/SHIPPED verdict block (RejectedShippedTriple) removed from both, per direct instruction — confirmed placeholders (a raw dev note left in the source Figma text layer, and FastRouter's own Side stepper/Top stepper/Unified form content, byte-for-byte identical between the two features and not customized for either). RejectedShippedTriple.tsx deleted once both call sites were gone.
- **2026-08-13** — Portfolio pushed to a real GitHub remote (Chienmayjoshi/portfolio) and deployed to Vercel. First Vercel build failed — root cause was a machine-level `~/.npmrc` (outside this repo) pointing npm at a private internal registry proxy, which got baked into every `resolved` URL in package-lock.json and was unreachable from Vercel's build container. Fixed by adding a project-level `.npmrc` pinning the public npm registry and rewriting all 441 resolved URLs to match (same packages/versions/integrity hashes, only the host changed — verified via a clean local build before pushing). `.claude/` (local Claude Code session config) added to `.gitignore` rather than committed, since it's machine-specific tooling state, not project content.
- **2026-08-20** — design-tokens.json's dark-mode `_PROPOSED` flags (bg-primary/bg-surface/bg-light/border-default/border-frame/text-accent/text-error/text-success, plus the underlying blue/red/green `.400` primitives) cleared — confirmed by inspecting Figma's own "Color" variable collection (VariableCollectionId:6263:8) directly via the Figma API, which already has full Light/Dark modes with every value matching code exactly. Matching `/* PROPOSED */` comments removed from globals.css's `.dark{}` block, and CouncilIntro.tsx's stale comment (which also cited an incorrect rgba value for the dark border-default token, corrected in the same pass) updated to match.
- **2026-08-22** — Started `fastrouter-slides` branch: a parallel, full-page horizontal-slide rebuild of the FastRouter case study (Figma section "portfolio case study interactive horizontal sliding approach", node 7213:122638), tested alongside the live vertical-scroll `/fastrouter` rather than replacing it. Added `primitive.font.mono` ("Geist Mono") and a new `semantic.typography.label-mono` token (Geist Mono Medium, 13px/18px, 0.78px tracking) rather than repointing the existing `label` token, since `label` is still used by the locked `/fastrouter` Hero eyebrow and CLAUDE.md forbids changing locked case-study rendering. The 0.78px tracking value (not the raw "6" the Figma variable defs reported) was confirmed against node 7032:61517's own generated code — same percentage-misread pattern already documented for the original `label` token on 2026-07-11. Also added a `.light{}` scope in globals.css mirroring `:root`'s light-mode color values, the mirror-image of the existing `.dark{}` override — needed because this new format's chapter-intro slides invert relative to whatever the global theme currently is, and CouncilIntro.tsx's existing scoped-`.dark` trick only covered forcing dark inside a light page, not the reverse.
- **2026-08-22** — TempHeader.tsx (sitewide placeholder scaffolding — "PREVIEW" text + theme toggle) and SlideHeader.tsx (fastrouter-slides-only header, built earlier the same day) unified into one global `Header.tsx` + `HeaderProvider.tsx` (both `src/components/shared/`), mounted once in `layout.tsx` and used sitewide, per direct instruction. No Figma frame exists for this navbar and no personal logo/wordmark exists anywhere in the repo — built as a text wordmark ("Chinmay Joshi", same treatment as the homepage `<h1>`) rather than a graphic mark, per direct instruction, function-first per CLAUDE.md's process for undesigned chrome. Center nav pill has two states, driven by `HeaderProvider`'s context (Header lives above the page tree at the root layout, so pages can't hand it props directly — same problem `ThemeProvider.tsx` already solves, pattern mirrored exactly): default sitewide nav (Work/AI Practices & Taste/About/Contact — "Work" is the only real link, same inert-text convention as Footer.tsx's own unresolved nav items) and a case-study identity pill (project logo · business · domain) shown once a reader is past that case study's intro. New `useCaseStudyIntroPill.ts` hook drives this via GSAP ScrollTrigger on `/fastrouter` (bound to the existing `id="intro"` on Hero, via a new `IntroPillTrigger.tsx`); `SegmentedRail.tsx` drives the equivalent for `/fastrouter-slides` directly off its own `activeId` state (no GSAP needed — chapter index is plain React state, not scroll position). Analytics/Extensions intentionally not wired this pass. Kept Header's height at the same ~64px TempHeader already established, since `fastrouter/page.tsx`'s `HEADER_OFFSET`, globals.css's `.practice-snap`, and `practice/BuildCard.tsx` all hardcode that number independently with no shared constant — changing it would have required updating all three in lockstep. Flagging one unreconciled copy inconsistency: the nav pill's "AI Practices & Taste" (inherited from SlideHeader's own Figma source) doesn't match Footer.tsx's separate "AI Practices"/"Design Taste" items — not fixed here.
- **2026-08-24** — HeroSlide.tsx (fastrouter-slides), two fixes flagged directly: (1) the meta grid's `gap-x-56px` referenced a spacing token that doesn't exist in this project's scale (...48, 60, 64... — no 56), so the column gap silently rendered as 0 and "Senior Product Designer"/"1 PM, 2 Engineers" ran together with no space. Corrected to the existing `gap-x-64px` token — same 56→64 rounding CLAUDE.md already documents for Council intro's identical off-grid Figma value, not a new judgment call. (2) The illustration band was absolutely positioned at 55% of the section's own height, independent of the text column's actual height — on real viewports where the text content is taller than 45% of the viewport, the two overlapped. Reworked to normal document flow (illustration now sits below the text block as a sibling, not layered under it) and made deliberately shorter than the text block above it, not an equal-height split, per direct comparison to the zainabkabira.com reference this whole format is modeled on.
- **2026-08-24** — HeroSlide.tsx reworked again, same day, per direct feedback: the illustration needs more height to read clearly (not the smaller fixed 260/380px band from the previous fix), and — since this is a horizontal-slide format where each slide is meant to fill exactly one viewport — the page must never scroll vertically, full stop. Section height changed from `min-h-screen` (a floor, content could grow past it and scroll) to a hard `calc(100dvh - 64px)` (`HEADER_HEIGHT_PX` constant in the file, matching the global Header's own height — the slide gets what's left after the header's real document-flow space, not the full 100dvh). Text column is now `shrink-0` (fixed at its natural height, `pt-64px pb-40px`, a consistent design-rhythm gap not tied to any one slide's content) and the illustration is `flex-1` (fills whatever's left, however much that is) — the "top padding stays fixed, the illustration is what adapts" resolves the "content height varies per page" question directly: a fixed top offset keeps every future slide's content starting at the same position, while the illustration organically gets more or less room depending on how tall that slide's own text content is, rather than a hand-picked pixel height per slide. If a second slide is added, `HEADER_HEIGHT_PX` should move to a shared location rather than being redefined per slide file.
- **2026-08-24** — SegmentedRail.tsx redesigned and a new ambient hero-zoom effect added, per direct reference to zainabkabira.com/work/fireflut's own case-study pager and hero-zoom mechanics — inspected directly (fetched the page's inline CSS, not guessed from the screenshot alone) rather than approximating. Mechanic reproduced, not copied byte-for-byte, adapted onto this project's own tokens: each chapter tick is now a small `h-16px` hit target (was `h-4px`, same height as its own visible line) capped at `max-w-60px` via `flex:1 1 0` (was full-width uniform flex-1 with no cap); read-so-far fill is `scaleX` per chapter (chapters before the active one now render fully filled, not just the active one — closes a gap in the original build, which only ever filled the currently-active segment); hovering any tick now thickens every tick's line together (`group` moved from per-segment to the shared `<nav>`) rather than each segment having its own isolated hover tooltip; the per-segment tooltip was replaced with one shared floating pill that glides between ticks via measured `offsetLeft` + `translateX`, matching the reference's reasoning that a per-tick pill would be clipped by its own small box. Bookend chapters (Hero, Closing) now drop their number in the pill, matching the reference's Overview/Closing/Read-next treatment — chapters `problem` through `evaluations` are numbered 01-06. Added `.hero-zoom-loop` (globals.css) — a plain CSS `@keyframes` (18s ease-in-out infinite, scale 1→1.05→1, respects `prefers-reduced-motion`) applied to a wrapper around HeroSlide.tsx's illustration images — confirmed via the reference site's own `.cs-hero-zoom` rule this is a continuous ambient loop, not scroll- or state-tied, so plain CSS rather than GSAP or Motion (neither this project's scroll-scrubbed nor React-state-tied animation category applies).
- **2026-08-24** — SegmentedRail.tsx, three more fixes flagged directly, same day: (1) track/fill colors switched from `bg-border-default`/`bg-text-accent` to dim-transparent-white (`bg-white/25`) for unread segments and solid white (`bg-white`) for read ones, both carrying a dark halo shadow (`shadow-[0_0_7px_rgba(0,0,0,0.4)]`) — the rail floats over the Hero illustration, not a flat page background, so it needs its own fixed contrast rather than a token tied to the global light/dark toggle. (2) Centering switched from `inset-x-0 + justify-center` (on a `w-full max-w-[760px]` child) to `left-1/2 + -translate-x-1/2` with an explicit `width: min(760px, calc(100% - 160px))` — the box math for the prior approach checked out by hand with no obvious bug, but with no browser available in this sandbox to confirm, adopted the reference site's own proven centering technique directly rather than ship an unverified fix. (3) Confirmed hover pill, line-thickening, and hit-target size were already correct per direct feedback — no change needed there. A custom cursor (present on the reference site) was explicitly deferred, not built.
- **2026-08-24** — Problem slide built (fastrouter-slides, Figma node 7039:61590, fetched directly). Headline/sub-headline/body copy pulled from Figma; the three pill cards use the original handoff doc's three distinct strings ("No visibility into spend," "No way to validate quality," "No trusted path to a verdict") instead of Figma's own copy, which repeats "No visibility into spend" identically three times — an obvious copy-paste artifact, not intentional design. Per direct instruction, the three pills render plain and side by side this pass; Figma's (and the doc's) stacked/offset depth-cue treatment is deferred, more detail to come. Body paragraph uses a literal 18px/30px (not the `body` token's 16px) — Figma's own variable-defs metadata for this frame lists `portfolio/body` as 16px but the node overrides it locally to 18px; flagged, not reconciled. Added `ModelMarqueeColumn` (in ProblemSlide.tsx) — two vertical columns of the 9 model names from the original handoff doc (which already exactly match Figma's own ghost-text names, Claude 3.5 Sonnet/DeepSeek V2.5, extended for variety), looping via new `.model-marquee-up`/`.model-marquee-down` CSS keyframes (globals.css) — same "ambient decorative loop -> plain CSS, not GSAP/Motion" reasoning as `.hero-zoom-loop`.
  - Real WebGL aurora shader added (`src/components/shared/AuroraShader.tsx`), per direct instruction after confirming lightswind.com/components/aurora-shader is itself WebGL-based (`ogl`/`WebGL`/`Shader` found in its fetched bundle) — building the real thing, not a CSS approximation. Hand-authored fragment shader (noise-perturbed flowing band, vertical fade), not reverse-engineered from that reference's minified source. **Blocked on `ogl` actually being installed** — this sandbox cannot reach `registry.npmjs.org` (confirmed via direct connectivity test; unpkg/jsdelivr and general web access work fine from here, so this reads as a deliberate package-registry restriction, not a general outage). Per direct instruction, shipping the rest of this slide without it rather than blocking on the dependency: the import and `<AuroraShader>` usage were removed from `ProblemSlide.tsx` (not deleted — `AuroraShader.tsx` is real, finished code, just currently unwired). Re-add both once `npm install ogl` has actually been run — `AuroraShader.tsx`'s own header comment says exactly what to restore. `npx tsc --noEmit` will keep showing one error on that file alone (unused but still on disk, still type-checked) until then; doesn't affect any actual route, since Turbopack only bundles what's imported.
  - Two real slides now exist, so bottom-rail navigation between them needed to actually work rather than stay deferred further: `fastrouter-slides/page.tsx` became the owner of a real horizontal `translateX` slide stage (Motion-driven, click-triggered — state/click-driven interaction, not scroll, per this project's animation split) — `HeroSlide`/`ProblemSlide` sit side by side in a `motion.div` sized to `slideCount * 100%`, each cell `100/slideCount %`. `HEADER_HEIGHT_PX` (the global Header's height) moved from `HeroSlide.tsx` (where it lived when Hero was the only slide) up to the page, since the stage now owns the height calc. `SegmentedRail.tsx`'s click handler switched from `scrollIntoView` (stopped making sense once slides left document flow for a translated stage) to an `onNavigate(index)` callback prop; `"problem"` added to `BUILT_CHAPTER_IDS`.
  - `Header.tsx`'s case-study pill swap (built instant on 2026-08-22) now animates: `motion.nav layout` lets Motion interpolate the pill's width automatically (FLIP) between its default and case-study states, `AnimatePresence mode="popLayout"` handles the content crossfade — added once a second slide made the swap actually visible in practice, not just theoretical. No new state, only how the existing `caseStudyPill` context value renders.
- **2026-08-24** — Two fixes flagged directly against a real screenshot. (1) `Header.tsx`: removed `bg-bg-surface/80 backdrop-blur border-b border-border-default` from the outer `<header>` — no background/border on the bar itself now (the center pill keeps its own). (2) `SegmentedRail.tsx`: found and fixed the actual centering bug this time, verified with real pixel measurements rather than reasoning about it in the abstract — the previous pass (same day) fixed the `<nav>` element's own position on the page (`left-1/2 + -translate-x-1/2`, confirmed correct via the compiled CSS and rendered HTML) but missed that the *inner* flex row holding the 8 ticks had no `justify-content` set. Since all 8 ticks hit their `max-w-60px` cap (8×60px + 7×6px gaps ≈ 522px, well short of the nav's 760px), they packed to the left edge of an otherwise-correctly-centered 760px container, leaving ~238px of dead space on the right — which is exactly why the rail read as narrower than expected and shifted left, even though the outer container was centered all along. Added `w-full justify-center` to the inner row. Verified by driving a real headless Chrome instance on this machine (`Google Chrome.app --headless --screenshot`, since no other browser automation is available in this sandbox) at both 1440×900 and 1920×1080, then measuring the active segment's pixel position directly with `sharp` — row midpoint lands within 1px of true viewport center at both widths. This is the first pass in this whole session where a visual claim was confirmed with actual pixel data instead of code review alone; worth reaching for the same technique before re-asserting "should be centered" on anything visual going forward.
- **2026-08-24** — Two more fixes flagged directly, same day, against a DevTools inspection of the live page (the person's own screenshot showed the header's inner row was a full-width `1920×74` `flex justify-between`, which is what exposed the actual bug). (1) `Header.tsx`: the center pill only lands on true page center with `justify-between` when the left (logo) and right (theme toggle) items happen to have equal widths — they don't ("Chinmay Joshi" text vs. a 40px circle — worked through the algebra: centered requires `logoWidth === toggleWidth`, which will basically never hold for arbitrary text). Switched the row to a 3-column CSS grid (`grid-cols-[1fr_auto_1fr]`) — two equal `1fr` flanking columns force the middle column (and the pill inside it) to true center regardless of the outer items' own widths, no manual width-matching needed. Verified via headless Chrome + `sharp`: pill's white background spans x741–1178 on a 1920px viewport, midpoint 959.5 vs. true center 960. (2) `SegmentedRail.tsx` now adapts its color scheme to whatever's behind it, per direct reference to zainabkabira.com's pager doing the same (`.cs-pager`/`.cs-pager.on-dark` swap `--pg-line`/`--pg-live`/`--pg-halo`/`--pg-pill` per the active band) — added a `theme: "on-dark" | "on-light"` prop (`RAIL_COLORS` lookup) rather than the single fixed white-on-dark-halo scheme from the previous pass, which only worked against Hero's busy illustration and would have been invisible against Problem's flat light background. `fastrouter-slides/page.tsx` now carries a `SLIDE_RAIL_THEMES` array parallel to `SLIDE_IDS` (Hero → "on-dark", Problem → "on-light") — the rail has no way to know its own background, so each slide reports which variant it needs. Verified by temporarily defaulting the slide stage to index 1 (Problem) for a screenshot, confirming the ticks render dark against the light background and are still correctly centered (x700–823 for the two solid segments, projected row midpoint ~961 vs. true center 960), then reverting the temporary default.
- **2026-08-24** — Two more fixes, same day, flagged directly against a real dark-mode screenshot. (1) The `SLIDE_RAIL_THEMES` fixed-per-slide array from the previous entry was itself wrong: it hardcoded Problem to `"on-light"` (dark ticks) without accounting for `bg-bg-primary` — Problem's actual background — being a token that flips dark in dark mode. In dark mode this produced dark ticks on a now-dark background (the "weird"-looking rail). Replaced the fixed array with `SLIDE_BACKGROUND_FOLLOWS_THEME` (`[false, true]`) plus a `useTheme()` read in `fastrouter-slides/page.tsx`: Hero's illustration is a static image that doesn't respond to the toggle, so it stays fixed at `"on-dark"`; Problem's token-backed background does respond, so its rail theme is computed live (`isDark ? "on-dark" : "on-light"`) instead of hardcoded. (2) `ProblemSlide.tsx`'s model-name marquee used Figma's literal `#D9D9D9` — subtle against Figma's light background, but bright/prominent against a dark one (light gray on near-black is high contrast, the opposite of subtle). Switched to the `text-muted` token, which is designed to read quietly in both themes. Verified via a real CDP-driven headless Chrome session (navigate → `localStorage.setItem('theme','dark')` via `Runtime.evaluate` → reload → screenshot, since `--blink-settings=preferredColorScheme` alone didn't reliably force it) — confirmed the rail renders bright/light against the dark Problem background and the marquee text reads as properly muted, not the previous flag-guessing approach.
- **2026-08-24** — Model-name marquee direction corrected against a Figma prototype the person built specifically to demonstrate the intended motion (the actual screen-recording they attached didn't come through — cached as a generic macOS file-type icon, not real video frames — so the Figma file itself, not the attachment, is what got inspected). `get_motion_context` returned nothing on every node tried (this animation isn't authored as Smart Animate keyframes that tool can parse), so fell back to raw `get_metadata` on three sequential frames sharing identical content ("problem statement", "problem statement1", "problem statement2") and measured each marquee column's x/y position directly at all three states. Every measured delta was an upward move — confirmed directly with the person that both columns should scroll the same direction, not the mirrored up/down approach originally built. `globals.css`'s `model-marquee-down` keyframe removed; `.model-marquee-up-slow` added (same `model-marquee-up` keyframe, 42s instead of 36s) so the two columns still drift out of phase with each other without actually reversing direction. `ProblemSlide.tsx`'s `ModelMarqueeColumn` prop renamed `reverse` → `slow` to match. A small (~21px) horizontal wobble was also visible in the measured keyframes but confirmed as placement noise, not a deliberate design decision — not reproduced; marquee stays pure vertical motion.
- **2026-08-24** — Found and fixed a genuine dev-environment bug, flagged directly ("left one marquee is scrolling, right isn't. the model names are repeating."): the source edit above (removing `model-marquee-down`, adding `.model-marquee-up-slow`) was correct on disk, but Turbopack's dev build cache never picked it up — confirmed empirically with a real time-lapse test (two headless-Chrome screenshots 4s apart, pixel-checksummed with `sharp`): the left column's checksum changed between frames, the right column's was bit-for-bit identical, i.e. genuinely frozen, not a screenshot-timing illusion. Traced to the compiled CSS chunk on disk (`.next/dev/static/chunks/[root-of-the-server]__1ul75re._.css`) still containing the old `.model-marquee-down` rule and no `.model-marquee-up-slow` at all — the right column's rendered class (`model-marquee-up-slow`) had no matching CSS rule to animate it, hence frozen; a touch and a full process restart of the dev server both failed to invalidate it, only `rm -rf .next` (full build cache wipe) followed by a fresh `npm run dev` produced a build with the correct rules. Re-ran the same time-lapse checksum test after the rebuild — both columns now change between frames. Also reduced marquee text opacity (`opacity-40` added on top of `text-text-muted`, `ProblemSlide.tsx`) — the token alone was still too prominent against the dark background per direct feedback.
- **2026-08-24** — The "repeating"/"list ends, needs to feel continuous" complaint persisted after the cache fix above — the frozen-column bug was real and worth fixing, but wasn't the whole story: `MODEL_NAMES` only had 9 entries, and on a tall viewport enough rows render at once that one full loop (9 unique + the duplicate copy the seamless-loop technique renders for `translateY(-50%)` to wrap cleanly) fits inside a single screen — row 1 and row 10 are literally the same name, visible simultaneously, reading as an obvious repeat even while genuinely animating. Expanded to 20 real, recognizable model names (kept the original 9, added GPT-4 Turbo, Claude 3 Opus, Gemini 1.0 Ultra, Llama 2, Mixtral 8x7B, Command R, Grok-1, Qwen 2, Phi-3, Falcon 180B, Titan Text — popular ones per direct instruction, not obscure) so the loop boundary sits further down than one viewport can expose. Verified via rendered-HTML count: all 20 names appear exactly 4 times each (2 columns × the 2 copies-per-column technique) — no accidental duplicates, no missing entries.
- **2026-08-24** — Still "cuts when names run out" even after the 20-name expansion — this time a genuine, different CSS layout bug, found by inspecting the actual live Web Animations API state (`element.getAnimations()` + `getBoundingClientRect()` via a real headless-Chrome CDP session) rather than reasoning about the CSS in the abstract: the animated inner div's height and its "masking" `overflow-hidden` parent's height were identical (2048px, the full 40-row content) — meaning `overflow-hidden` had nothing left to clip. Root cause: `ModelMarqueeColumn`'s masking wrapper sets `h-full`, but its parent (the flex row holding both marquee columns and the centered text block, `ProblemSlide.tsx`) had no explicit height of its own — a percentage height (`height:100%`) against an auto-sized ancestor resolves to `auto`, not a real pixel value, so the "mask" silently became exactly as tall as its own content instead of a small fixed clipping window. The list wasn't failing to loop — it was never actually being clipped/scrolled inside a bounded box at all, so once you'd scrolled through it you'd see blank space where it "ran out." Fixed by adding `h-full` to that row div, giving the whole ancestor chain a real height to resolve against. Verified two ways: (1) re-ran the same `getBoundingClientRect()` inspection — masking parent now reports 929px (a real, independent height) against 2048px of content, `clipping: true`; (2) screenshot at t=0 and again 8s later showed the left column having already scrolled past "Titan Text" (the last name) and wrapped cleanly back to "GPT-4o," fully filled top to bottom with no blank gap at any point.
- **2026-08-24** — Two additions to the Problem slide, both driven by a Figma update to node 7039:61590, inspected directly (metadata + a fresh screenshot). (1) New `src/components/shared/GridDepthLayer.tsx` — a subtle grid-texture decoration (Figma node 7239:5686: a dense tiling of ~400+ individual 44.44px square vectors layered with an 800×400 ellipse that reads as a radial fade). Reproduced as pure CSS — two `repeating-linear-gradient` hairline layers plus a `radial-gradient` mask — rather than exporting/tracing the vector grid; same visual result, no image asset. Line color is the existing `border-default` token, not a new value. Built generic (placement controlled by the caller's `className`) per direct instruction that this is meant for reuse across other case-study pages, not just Problem — only wired into Problem this pass, where else it lands is a future call. (2) `ModelMarqueeColumn` restructured from straight columns into Figma's zigzag: measured the node directly and found each row alternates flush-right/flush-left within the column (`x + textWidth == columnWidth` on the "right" rows, `x = 0` on the "left" ones) — a clean, deterministic alternation, not freeform scatter, reproduced via `text-right`/`text-left` on `i % 2`. Column width `180px` → `240px` on both columns per direct instruction — Figma's own right column is still `300px` in this snapshot, an unintentional inconsistency (not a second deliberate size), standardized to `240px` throughout. Verified via headless-Chrome screenshot (temporarily defaulting the slide stage to Problem, same technique as every prior pass) that both the grid's center fade and the zigzag alternation render as intended.
- **2026-08-24** — Marquee tuning per direct feedback: durations slowed (`globals.css`, `.model-marquee-up` 36s→50s, `.model-marquee-up-slow` 42s→60s), text opacity reduced further (`opacity-40`→`opacity-30`), row gap widened (`gap-32px`→`gap-40px`), both in `ProblemSlide.tsx`. Hit the same Turbopack stale-build-cache bug as the 2026-08-24 direction fix earlier — the compiled CSS chunk kept serving the old 36s/42s durations after the source edit, confirmed by fetching the compiled chunk directly rather than trusting the dev server. `rm -rf .next` + a fresh `npm run dev` fixed it again, same as before. Noting this because it's now happened twice in one session on CSS-only edits to this same file — if a CSS-only change to `globals.css` doesn't seem to take effect, check the compiled chunk directly before assuming the source edit is wrong.
- **2026-08-24** — "The Problem" heading (`ProblemSlide.tsx`) got a direct `pb-40px` for extra breathing room below it, separate from the uniform `gap-24px` the rest of that column already shares.
- **2026-08-24** — `SegmentedRail.tsx`'s hover pill, two fixes flagged directly against a screenshot. (1) The pill was positioned via `left-0 + translateX(tick.offsetLeft)` — pinning the pill's own left edge to the hovered tick's left edge, not centering it. Since the pill is much wider than a 60px tick, this read as shifted right rather than centered. Fixed by tracking the tick's *center* (`offsetLeft + offsetWidth / 2`) and combining it with the standard self-centering transform: `translateX(calc(Npx - 50%))` — the `-50%` centers on the pill's own (variable, per-label) width automatically, no separate measurement of the pill needed. Verified with a real simulated hover (CDP `Input.dispatchMouseEvent` at the tick's actual screen coordinates, not just a static screenshot) and pixel measurement: pill span 731–858, midpoint 794.5, against the hovered tick's true center at 795 — under 1px off. (2) The number-to-label gap and the divider's height were `gap-11px`/`h-11px` — 11px isn't a defined value in this project's spacing scale (`globals.css`'s `--spacing-*px` set jumps straight from 10px to 12px), so both were silently no-op utilities: zero gap, and a zero-height (invisible) divider — the same "undefined spacing token" class of bug already caught twice elsewhere this session (`gap-x-56px` on the Hero meta grid, `gap-x-56px` again on... — worth remembering this pattern generally: any `-Npx` utility should be checked against the actual defined scale, not assumed to exist just because the syntax looks valid). Fixed to `gap-16px` (more than the nearest valid token, 12px, since more space was explicitly requested) and `h-12px` (the divider's correct nearest valid height) — confirmed visually in the same hover screenshot: "01 | Problem" now reads with a clearly visible divider and real spacing.
- **2026-08-24** — Both `fastrouter-slides` slides had a real (if small) vertical page scroll, flagged directly ("we dont need it"). Traced with a real browser session (`document.documentElement.scrollHeight` vs. `window.innerHeight`): a consistent 10px overflow on both Hero and Problem. Root cause: `fastrouter-slides/page.tsx`'s slide-stage height (`calc(100dvh - Npx)`) hardcoded `N = 64`, carried over from `TempHeader`'s old height math and never re-verified against the current `Header.tsx` — measured `Header`'s actual rendered height directly and it's 74px (the center pill's text content, no explicit line-height set, ended up taller than the 40px `ThemeToggle` it was originally sized against). Rather than just correct the constant to 74 (which would silently break again the next time `Header`'s content changes height — a longer pill label, a font tweak), replaced the hardcoded constant with a live measurement: a `useEffect` queries `document.querySelector("header")` directly (`Header` lives in the root layout, a sibling ancestor with no normal React ref path from this page) and tracks its height via `ResizeObserver`, falling back to the old 64px guess only for the SSR/first-paint frame before the effect runs. Verified via the same `scrollHeight`/`innerHeight` check on both slides post-fix: `overflowAmount: 0`, `canScrollY: false` on both.
- **2026-08-24** — `Header.tsx`'s left slot is now dynamic per route, per direct instruction: case-study pages (`/fastrouter`, `/fastrouter-slides`, `/analytics`, `/extensions` — matched via `usePathname().startsWith(...)`, `/fastrouter-slides` covered by the same `/fastrouter` prefix) show "← Back" (to `/`, the project listing — you're leaving one project, not browsing the index) instead of the "Chinmay Joshi" logo, which stays for every other route (`/`, `/practice`). Route-matched rather than given the same per-page context-registration machinery the center pill uses (`HeaderProvider`) — this only depends on which page you're on, never on scroll/slide position within one, so the lighter `usePathname()` check is sufficient. Also: new standing instruction to use Phosphor icons (`@phosphor-icons/react`) for any icon work going forward — attempted to install it for this exact back-arrow icon and hit the identical `registry.npmjs.org` connection block `ogl` did for `AuroraShader.tsx`. Unlike `AuroraShader` (scoped to one unwired component), `Header` is global — every route renders it via the root layout — so leaving a broken import here would 500 the entire site, not just one slide. Shipped with a temporary hand-rolled inline SVG (matching `ThemeToggle.tsx`'s existing icon convention) instead, clearly flagged in `Header.tsx`'s own comment to swap for the real `ArrowLeft` from `@phosphor-icons/react` once `npm install @phosphor-icons/react` has actually been run. Verified all six routes serve the correct left-slot content (200 on every route; case-study routes render "Back", the rest render the logo) and confirmed no scroll regression from Header's content change (the `ResizeObserver` height-measurement fix from the entry above self-corrected automatically, no manual re-tuning needed).
- **2026-08-24** — Mobile-responsive pass on the `fastrouter-slides` Hero/intro screen, against a new Figma frame (node `7255:6865`, "case study intro-mobile," 390×864), preceded by three explicit confirmations before building: (1) the new hamburger icon opens site-wide nav (Work/AI Practices & Taste/About/Contact) — the mobile home for what the header pill shows inline on desktop; (2) the floating "SECTION 0X | Chapter" pill replacing the desktop rail on mobile is passive status only, not tappable; (3) mobile uses the same full-viewport slide-paging architecture as desktop, not the original handoff doc's "continuous vertical scroll" — that doc predates this Figma frame, and per CLAUDE.md's own rule, Figma wins on conflict. Four files touched: `Header.tsx` (icon-only Back below `md:`, center pill's default site-nav branch now `hidden md:flex` — mobile never shows it inline, real case-study-identity content unaffected; new hamburger button reusing `ThemeToggle`'s own `size-40px rounded-lg border-border-default bg-bg-surface` chrome per Figma's matched-pair treatment, opening a function-first dropdown — no Figma "open" state exists to match, `NAV_ITEMS` extracted once so desktop pill and mobile dropdown can't drift out of copy sync); `HeroSlide.tsx` (mobile-only `GridDepthLayer` — genuinely new on mobile, not a responsive resize of an existing desktop layer, since desktop's Hero node `7032:61508` has no equivalent element at all, confirmed directly rather than assumed; `px-20px` mobile side padding and `gap-24px` mobile meta-grid gap, both measured off the real Figma frame; headline base-breakpoint size corrected from an unverified pre-Figma guess, `36px/42px/-0.6px`, to the real measured value, `32px/40px/-0.32px`, node `7255:7302` — TL;DR paragraph and meta-grid text sizes checked too and turned out byte-identical to what was already hardcoded, so left unchanged); `SegmentedRail.tsx` (8-tick rail now `hidden md:block`, new `md:hidden` always-visible floating pill reusing the same number/divider/label markup shape, literal-hex colors matching `RAIL_COLORS`' own reasoning — floats over Hero's busy illustration, needs fixed contrast, not a theme-linked token; copy for the pill sometimes differs from the desktop rail's own chapter label — verified for "hero" specifically, whose mobile pill reads "01 | Case Study Intro" versus the desktop rail's bare/unnumbered "Hero" — handled via a small `MOBILE_CHAPTER_OVERRIDES` map rather than assuming every other chapter's mobile copy matches desktop, since no other chapter has a mobile Figma frame yet to check against). No new tokens added to `design-tokens.json` — this slide's headline already uses inline arbitrary sizes rather than the named type scale (documented in its own header comment), so the mobile correction followed the same existing convention rather than getting promoted to a token. Verification caught one real methodology trap, not a code bug: an early screenshot via headless Chrome's `--window-size=390,844` CLI flag appeared to show the headline overflowing/dropping a word — investigated via CDP (`Emulation.setDeviceMetricsOverride`, `Runtime.evaluate` measuring the actual DOM rects and text-node line boxes) and found the CLI flag doesn't reliably set the true CSS viewport at narrow widths (it reported `window.innerWidth` as 500, not 390); the properly-emulated 390px render wraps the headline correctly into 3 lines with no overflow. Left a defensive `w-full` on the h1 (harmless, doesn't change any confirmed-correct rendering) but corrected the inline comment that had drawn a false conclusion from the flawed screenshot before double-checking. Final verification: `npx tsc --noEmit` clean (only the standing, unrelated `ogl` error); real mobile screenshots (light + dark, via CDP) show correct header/hamburger/content/grid-layer/floating-pill; hamburger dropdown opens/closes via a real simulated click; desktop (1920×1080) and the live `/fastrouter` route both re-screenshotted and confirmed unaffected.
- **2026-08-24** — Follow-up round on the mobile pass above, prompted by a screenshot showing the header's right-side icons (theme toggle + hamburger) not flush against the right edge, plus a note that the intro Figma frame (node `7255:6865`) had since been revised to a genuinely more compact spec ("spacing reduced to occupy most of content") and a brand-new mobile Problem frame (node `7249:6413`, "problem statement-mobile") to build against. Three real fixes: (1) **Header right-alignment bug, root cause found via CDP, not guessed**: `getComputedStyle` on the grid reported `grid-template-columns: 129px 92px 129px` (all three tracks present) but the icon group's own rect sat inside the *middle* track's x-range, not the last one. Cause: CSS Grid's implicit auto-placement drops a `display:none` item (the center nav, hidden below `md:` whenever there's no case-study pill) from placement entirely rather than collapsing its track to zero — the two visible children then auto-place into the first two tracks in DOM order, leaving the true rightmost track empty and the icon group stuck in the middle one. Fixed with explicit `col-start-1/2/3` on all three direct children of the header grid, so a hidden sibling can no longer reflow the others — confirmed via the same CDP measurement post-fix: `175px 0px 175px`, icon group's right edge at `x=370` (`390 - 20px padding`, exact). (2) **`HeroSlide.tsx` mobile spacing, re-measured via `get_metadata`'s raw coordinates** (the earlier `get_design_context` codegen pass hadn't surfaced that the content rhythm was tighter than desktop's, not just re-flowed at the same rhythm): content top padding `40px` (was `64px`), eyebrow-to-headline gap `24px` (was `40px`), headline-to-paragraph gap `24px` (was `32px`), the TL;DR paragraph's own vertical padding `32px` (was `40px`), and — measured by exact addition, not assumed — zero gap between the text block and the meta grid below it (was `40px`; the meta grid's Figma y-position lands exactly where the text block's own bottom edge ends). All applied as mobile-base values with `md:` overrides restoring the original desktop numbers unchanged. (3) **New mobile Problem slide** (`ProblemSlide.tsx`): top-anchored layout on mobile (`items-start`/`pt-40px`) vs. desktop's vertical centering — a real architecture difference per node `7249:6791`, not a rescaled version of the centered layout; headline/subheading/paragraph sizes corrected to real measured values (`32/40/-0.32`, `24/32/-0.1`, and a `28px` line-height on the paragraph — none of which matched the pre-existing base-breakpoint fallbacks, which predated any mobile Problem frame); new `ProblemPillStack` component (mobile-only) reproducing the fan/depth-cascade three-card treatment the original handoff doc specified and desktop's own header comment already flagged as deliberately deferred — built scoped to mobile only, desktop's flat row left untouched. Reused the same three distinct `PROBLEM_PILLS` strings already established for desktop's row rather than Figma's repeated "No visibility into spend" ×3 placeholder (same copy-paste artifact already flagged once in this file). Border colors (`#D9D9D9`/`#6B6B6B`/`#0D0D0D`, a three-step depth cascade) are literal hex, not tokens — this project's border scale only has two steps — and are **flagged, not fixed, for dark mode**: the front card's near-black border nearly disappears against a dark `bg-bg-primary`, confirmed in an actual dark-mode screenshot, and no dark-mode version of this Figma frame exists to check a corrected treatment against. Also caught and fixed a second real bug while testing Problem on mobile: `Header.tsx`'s center pill had been made visible below `md:` "once a case-study identity exists" — untested at the time, since Hero (the only slide tested so far) never sets one. Once Problem's real `caseStudyPill` context value populated it, the full desktop-width pill broke the 390px header layout. Neither mobile frame checked (Hero's or Problem's) has ever actually shown a populated center pill — both keep it empty — so corrected to unconditionally `hidden` below `md:`, matching what's actually verified rather than an extrapolation beyond it. Also flagged, not silently resolved: Problem-mobile's floating status pill copy in Figma reads "SECTION 04 | Feature Overview" on a screen that is visibly the Problem statement — looks like a copy-paste leftover from a different chapter's frame, not intentional; left `SegmentedRail.tsx`'s `MOBILE_CHAPTER_OVERRIDES` map without a `problem` entry so it falls back to the desktop rail's own correct "01 Problem" label instead of reproducing what looks like a Figma authoring mistake. Verification: `npx tsc --noEmit` clean (same standing `ogl` error only); mobile Problem screenshots taken by calling `.click()` directly on the desktop rail's (hidden-on-mobile, but still present and functional) `button[aria-label="Problem"]` via CDP `Runtime.evaluate` — mobile has no swipe/tap chapter-navigation mechanism built yet (still an explicitly open item from the original mobile-responsive plan), so this was the only way to reach the Problem slide for a real mobile screenshot; both slides re-verified light + dark via CDP device-metrics emulation, desktop (1920×1080) and the live `/fastrouter` route re-screenshotted and confirmed unaffected.
- **2026-08-24** — Closed the mobile chapter-navigation gap flagged in the entry above ("not able to slide the intro page to the problem page on mobile"): swipe added to `fastrouter-slides/page.tsx`'s slide stage, scoped to mobile only (`isMobile`, a live `matchMedia("(max-width: 767px)")` listener) — desktop keeps its existing click-through-the-rail navigation only, since it already has a full mechanism and enabling drag there risked fighting text selection in the paragraph copy for no benefit. Required a real restructure, not just adding a `drag` prop: the stage's `x` was previously driven by percentage strings (`-${activeIndex * (100/length)}%`) via a declarative `animate` prop, but Motion's `dragConstraints` only accepts pixel bounds, and mixing percentage and pixel values on the same motion value doesn't reconcile cleanly. Switched to `useAnimation()` controls driving `x` in measured pixels (`stageWidth`, this container's own rendered width via `ResizeObserver` — each slide cell is exactly one viewport width regardless of slide count, so it's the correct per-slide step) and a single `goToSlide(index)` entry point used by both the rail's clicks and swipe's drag-end, which always issues an explicit `controls.start(...)` rather than relying on a declarative `animate` target changing — that distinction matters specifically for a drag that ends short of the swipe threshold: the index doesn't change, so a declarative prop wouldn't see a different target and wouldn't snap the dragged-away position back, while an imperative call fires regardless. Swipe commit threshold: 50px offset or 500px/s velocity, whichever comes first (`SWIPE_OFFSET_PX`/`SWIPE_VELOCITY_PX_PER_S`) — a fast flick or a slow-but-far-enough drag both count, matching how native carousels feel; `dragElastic={0.15}` for light resistance past the first/last slide, `dragMomentum={false}` so Motion's own inertia doesn't carry the position past the controlled snap target (the snap logic is handled entirely in `onDragEnd`, not left to Motion's default drag physics). Also added a resize-correction effect (`controls.set`, not `.start` — instant, no animation) for the case where `stageWidth` changes after a navigation already happened (device rotation, window resize), so the stage doesn't end up misaligned relative to the new width. Verified via CDP mouse-event drag simulation (dispatched as a `mousePressed`/several `mouseMoved` steps while pressed/`mouseReleased` sequence — Motion's drag responds to pointer events, which Chrome synthesizes from these) at a true 390px mobile viewport: a 200px leftward drag correctly advanced Hero → Problem; a 15px drag (under the 50px threshold) correctly snapped back with no change; a 200px rightward drag correctly returned Problem → Hero. `npx tsc --noEmit` clean (same standing `ogl` error only).
- **2026-08-25** — Reworked `fastrouter-slides` mobile navigation from the horizontal swipe (previous entry) to **free vertical scroll**, flagged directly ("swipe should be vertical," then — once the variable-viewport-height problem surfaced — "just allow users to scroll freely"). The split between the two experiences moved from viewport width to **input type**: `isTouch` is now a live `matchMedia("(hover: none) and (pointer: coarse)")` listener (the standard touch-primary test) rather than `max-width: 767px`, because a landscape phone or tablet is wide (≥768px) but still wants the vertical experience — the old width split handed those the pointer carousel and hid the pill, flagged directly. Touch renders a normal-flow vertical stack (each slide content-height, no swipe/drag); the bottom status pill follows scroll via an `IntersectionObserver` "scroll-spy" (`page.tsx`) instead of the rail. The **same `isTouch` signal is passed to `SegmentedRail` as a `variant` prop** ("rail" | "pill") so its pill-vs-rail choice can't disagree with which stage is actually rendered — the old `md:`-width CSS toggle on the rail/pill was replaced by this prop, since a width-based rail floating over a touch vertical stack would render nav that navigates nothing. Pills got fixed widths: the mobile status pill `w-[200px]` (Figma node `7275:427`, so a changing scroll-spy label can't reflow the pill width), and — after a round-trip — the **desktop hover pill kept content-width** (it only appears on hover, one label at a time, so there's no flicker to guard against). Scroll-spy detection band went through two corrections (center → 20–30% from top → finally `-70% 0px -25% 0px`, low in the viewport) so the label flips as a section *starts* entering rather than once it already fills the screen — the placement is counter-intuitive (a band near the top fires late because a section must scroll almost fully up to reach it). Verification limited by the offscreen automation tab (documented repeatedly below): can't drive a true mobile CSS viewport or fire `IntersectionObserver`/programmatic-scroll events, so the scroll-spy *timing* was confirmed only by reasoning + forcing the touch branch via a patched `matchMedia`; structural checks (pill width, branch rendering, 3 slides) verified in-browser.
- **2026-08-25** — Built the **Product slide** (third `fastrouter-slides` chapter, "What is Fastrouter?"), Figma desktop node `7070:61815` / mobile `7275:2`, file `2aoIFdaJMyNBEWeQESBEzG`, via the figma-implement-design skill. New `ProductSlide.tsx` (responsive single component like Hero/Problem): reuses `GridDepthLayer` behind the headline and `ScreenshotFrame` for the product shot; typography verified per-node (headline Season Mix 56/64/-0.56 desktop, 32/40/-0.32 mobile; subhead Google Sans Flex SemiBold 28/40 / 24/32; body 18/30 both). Wired into `page.tsx` (`SLIDE_IDS`/`SLIDE_COMPONENTS`/`SLIDE_BACKGROUND_FOLLOWS_THEME` — Product is `bg-bg-primary`, so `true`/follows theme like Problem). `SegmentedRail.tsx` renumbered/relabelled to the Figma "floating section pill" scheme (nodes `7255:7281`/`7249:6818`/`7275:427`): sequential from Hero, reworded — `01 Introduction · 02 The Problem · 03 The Product · 04 Feature Overview · 05 Council · 06 Observability · 07 Evaluations · Closing`. Only the four with a real mobile frame are confirmed copy; the rest are renumbered but keep existing labels (no invented reworded copy — CLAUDE.md). `MOBILE_CHAPTER_OVERRIDES` removed (mobile and desktop labels have converged), `BUILT_CHAPTER_IDS` += `product`. Copy artifact flagged, not fixed: the body's "controlsurface" (missing space, meant "control surface") appears on both desktop and mobile Figma frames — reproduced verbatim (Figma is canonical for FastRouter copy). Downloaded the landing-page still from Figma node `7071:61927` (3448×1468) and converted to `fr-product-web-hero.webp` (69KB via `sharp`); no dark variant exists in Figma, flagged.
- **2026-08-25** — Product representation reworked twice per direct feedback. (1) "Build it exactly as design": replaced the shared `ScreenshotFrame` gradient bezel with Figma's own treatment (nodes `7071:61924`/`7279:434`) — a **grey `bg-bg-light` container** (Figma `#f5f5f5`, top corners rounded 24, flips in dark mode), a `border-border-default` browser mockup inside, and the screenshot fading off the **bottom edge** (a full-width vertical fade, not the bezel's corner mask). Body copy split into two `<p>`s so the line breaks after "…runs through it." (matching Figma's own two-`<p>` split). A per-slide treatment rather than a fork of the shared frame, per the request. (2) Then the still became a **video**: the actual asset is a screen recording (Figma uses a video there too), so swapped in the shared `ThemeAwareVideo` with the existing product-hero clips (`fr-product-hero.mp4` / `fr-product-hero-dark.mp4`, the same ones the live route's `ProductBrowserMockup` uses) — this also gives light/dark handling for free (one `<video>`, src from `ThemeProvider`), so the earlier "no dark variant" gap is gone; the unused `fr-product-web-hero.webp` was deleted. The bottom-fade target changed `bg-light` → `bg-primary` (fade into the page colour, per ask), and the mockup corner radius `8px` → `24px` (per ask). Inline gradient for the fade, matching the codebase's convention (`ScreenshotFrame`/`GridDepthLayer`).
- **2026-08-25** — A run of smaller `fastrouter-slides` refinements, each flagged directly. (a) **`GridDepthLayer` cell size made responsive** — the hardcoded 44px tiling unit became a `--grid-cell` CSS variable (default 44px, so Hero/Problem are untouched), overridable per breakpoint from the caller's className; Product uses `[--grid-cell:24px] md:[--grid-cell:40px]` (24 was first applied to both, then corrected to mobile-only with 40 on desktop). A single-number prop couldn't do this because inline style can't hold media queries. (b) Product **headline left-aligned on mobile** (matching Problem), centered on desktop — overrides Figma node `7275:390`'s `text-center`. (c) **Mobile status pill made theme-adaptive** (`SegmentedRail.tsx`): stayed dark on a now-dark background in dark mode; added `dark:` overrides so it inverts to a light pill (white bg, dark ink) in dark mode — light values mirror the desktop rail's `on-light` pair, dark values `on-dark`. (d) **Touch stack fixed 120px inter-slide gap** — the earlier `min-h-[calc(100dvh-header)]` per slide left large, content-dependent empty gaps on short slides; replaced with content-height slides + a constant `gap-[120px]` (verified exactly 120px between all three). (e) **Header mobile background** (`Header.tsx`) — scrolling content collided with the bare icons; a backdrop-blur (Figma "scrolled state" node `7284:440`) was tried first but looked wrong with a line of text half-under it, so replaced with a **mobile-only gradient scrim** that fades `bg-primary` (opaque, behind the icons) → transparent, dissolving rising content into the page colour. Token-based (flips light/dark), `md:hidden`, no scroll-position logic (the fill is the page colour at the top so it's invisible there and only reads once busier content passes behind it).
- **2026-08-26** — First production deploy of the above. Build blocker resolved per direct instruction ("ignore the ogl error"): added a targeted `src/types/ogl.d.ts` ambient stub (`declare module "ogl";`) so Next 16's whole-project type-check passes — `AuroraShader.tsx`'s dynamic `import("ogl")` is the only site, and that component is orphaned (referenced only in comments), so this silences just TS2307 without disabling type-checking anywhere else (kept over `ignoreBuildErrors: true`, which would blanket-disable it, and over installing `ogl` for unused code). Verified a real `npm run build` (green, all 12 routes including `/fastrouter-slides`) before pushing. Committed everything since the last hosting commit (`40cfd1d`, 21 files) and pushed to `main` → Vercel production. The push initially failed from this session's shell (no valid GitHub credential in the environment — no `gh`, no SSH key, stale osxkeychain token); completed by the user from their own Terminal with a personal-access token.
- **2026-08-27** — **Desktop deck rearchitected from wheel-hijacking to native scroll.** The goal was scroll-to-navigate on the pointer/desktop deck (rail-click was the only nav). First attempt hijacked `wheel` (window listener, `{ passive: false }`, a lock so one gesture = one slide) — but it was unreliable on macOS trackpads: "doesn't slide until I click the page." Successive fixes each failed to fully solve it (element-scoped → window listener; `overscroll-behavior: none`; focus-the-stage-on-mount) — root cause is that the page was exactly one viewport tall (nothing to scroll), so the browser handed the trackpad gesture to the compositor as a rubber-band overscroll and only "latched" scrolling to the page after a click. Keyboard nav (Arrow/PageUp/PageDown/Space) was added along the way and worked. Final fix, per direct instruction ("try native scroll driven slide, check how zainab is doing it") — inspected zainabkabira.com's own case studies and confirmed they're plain **native vertical scroll** (`scrollHeight` ~16× viewport, `body { overflow: hidden auto }`, no scroll-jacking, no CSS snap), which is why they're reliable. Adapted to FastRouter's horizontal design: `page.tsx`'s pointer branch is now a **real scroll container** (one viewport of scroll length per slide) whose scroll position drives a pinned (`position: sticky`) horizontal row's `translateX` — written imperatively to the row ref every scroll frame (no React re-render, so it survives `activeIndex` re-renders), with `activeIndex = round(progress)`. CSS `scroll-snap-type: y mandatory` + `scroll-snap-stop: always` on per-slide markers gives one slide per swipe; `overscroll-contain`, hidden scrollbar. Native scroll is delivered by the browser unconditionally — no click, and wheel/trackpad/keyboard/scrollbar all work for free. The Motion carousel, its `useAnimation` controls, `stageWidth` measurement, and all the wheel/overscroll/focus hacks were removed; `navigateTo` (rail click + keyboard) is now a smooth native `scrollTo`, so scroll position is the single source of truth. Verified structurally in-browser (container scrollable; scrollTop 0 → −1× → −2× width transform maps exactly to Introduction/Problem/Product; sticky pin, `y mandatory`, `snap-stop: always`, hidden scrollbar all present) — but the offscreen automation tab doesn't dispatch `scroll` events for programmatic scrolling, so the real-gesture feel needs an on-device check; native scroll firing on real user input is guaranteed browser behaviour, so this is the reliable end state. Committed (`3e8eff6`) and pushed to `main` → production (auth succeeded this round).
- **2026-09-01** — Built the **Reframe slide** (`ReframeSlide.tsx`, closing the Council intro stack), Figma desktop `7187:98325` / mobile `7322:16130`. Four deviations. (1) **Single markup, not duplicated breakpoint blocks** — unlike the earlier Council slides, the two frames are structurally identical (same nodes, same order); only the type scale and column width differ (730/56/28 desktop vs 350/32/20 mobile), so this is one tree with `md:` variants rather than the desktop+mobile pair the Brief/Conceptual Grounding slides genuinely need. (2) **Brackets rebuilt, not transcribed** — Figma draws each as three 4px bars (a 224px stem + two 40px arms) inside a 40×226 box; reproduced as one div with 4px borders on three sides, and sized `self-stretch` rather than a hardcoded 226px so they still wrap the statement at widths Figma has no frame for. The text carries Figma's own vertical padding (31px desktop / 13px mobile), which reproduces the exact 226px at the Figma widths. (3) **Figma contradicts itself on the struck-through FROM ink** — desktop frame `#AAA`, mobile frame `#6B6B6B` (= `text-muted`). Took the token: theme-aware, and exact against the mobile frame. Desktop therefore reads slightly darker than its Figma render. (4) Mobile frame binds the body to DM Sans via `decision-title`; that's the known-stale binding (CLAUDE.md), so Google Sans Flex at the same 20/26/-0.1 metrics. Also flagged, not fixed: the mobile frame's floating pill reads "04 · The Reframe" while `SegmentedRail`'s table has the Council chapter at 05 — the pill artwork appears to predate the renumbering, so the slide follows its siblings (`council`) rather than the frame.
- **2026-09-02** — **Hero slide desktop padding corrected against the header**, flagged directly ("the intro page content got pushed up… feels very close to the header"; "40px below the description is enough"). The 2026-08-27 seamless-header change pulls every slide up behind the transparent Header (`marginTop: -headerHeight`), which silently reinterpreted the existing flat `md:pt-64px` as 64px from the *viewport* top — i.e. underneath a ~74px header. Figma's own frame (`7032:61508`) puts the header row at y24/h54 (bottom 78) and the content block at y130: the spec is a **52px gap below the header**, not an absolute offset. Rebuilt as `md:pt-[calc(var(--fr-header-h,0px)+48px)]` so the relationship survives any header height (52 → the 48px scale token, same nearest-token treatment used elsewhere). Bottom padding went to `md:pb-0`: Figma's content block ends at y548 and the illustration frame starts at y548 — no gap at all — so the 40px below the description is the description block's *own* `py-40px` (node `7032:61533`: 40 + 56 text + 40), which the wrapper's `pb-40px` had been doubling to 80. Mobile keeps `pb-40px` (the touch stack isn't pulled up behind the header). Measured live afterwards: 48px under the header, 40px under the description.
- **2026-09-02** — **Conceptual Grounding tweet bottom fade rebuilt as a mask**, flagged directly ("the tweet doesn't have a bottom fade similar to the product page… this is there even in the figma design"). The fade already existed but was invisible *by construction*: an overlay div filled with a gradient to the card's **own** background — white fading into white, on a card whose content ends inside it, so there was nothing to dissolve into. Figma (`7183:97478`) dissolves the whole card — border, surface and engagement row together — so this is now a `mask-image` on the card root, this codebase's established faded-edge mechanic. Deviation from `ScreenshotFrame`'s shared ramp: `black 72% → transparent 97%`, not `82% → 100%`. ScreenshotFrame fades a screenshot sitting inside a bezel, where a soft tail is fine; here the thing being faded is the card's own border and rounded bottom corner, and reaching zero only at the last pixel row left that edge faintly drawn — reading as a *clipped* card rather than a dissolving one. Verified in both themes; the mobile card is untouched (height-clipped with its own fade-to-page-bg from the caller).
- **2026-09-02** — **Council decisions 02–04 built** (`DecisionSlide.tsx`), Figma `7202:100116` / `7202:100926` / `7202:101754`, completing the chapter at four. Template extended with two optional slots and an asset kind, plus one correctness fix. (1) **`subtitle?`** — an accent-bordered line under the headline, which only decision 02 has ("Arguments → Peer Review → Verdict"); identical treatment to decision 01's pull-quote, different slot, so both now share one `AccentLine`. **`quote?`** became optional — only 01 has one. (2) **`asset.kind`** — 02 and 04 are screen recordings, not stills (both decisions are about motion over time), so they route through `ThemeAwareVideo` while 01/03 keep `ThemeSwap`; same split and same files as the vertical route. (3) **One capsule, reordered — not one per breakpoint.** The original template rendered it twice (`md:hidden` + `hidden md:block`), which with video means two autoplaying decoders per slide, and the pointer deck keeps all 12 slides mounted so nothing unmounts offscreen. Replaced with the `contents` + `order` technique already used for this exact layout in `FiveDecisions.tsx` (see 2026-07-12). Decision 01 renders identically. Three things flagged rather than silently resolved: **(a) no mobile Figma frames exist for 02–04** (01 has `7295:1742`), so mobile is the template's treatment applied to all four — verified against 01's frame, not four pixel matches; **(b) Figma contradicts itself on the third rationale label** — 01's frame says "WHY?", 02/03/04 all say "WHY", as does the shipped vertical route; normalized to "Why" on that majority, and 01's frame is the outlier to fix in Figma; **(c) the slide frames' copy differs from the vertical route's wording of the same decisions** (e.g. 02's "Chose" names all three stages in the slide frame and doesn't in `FiveDecisions.tsx`) — the slide frames are canonical for the slides, so the two were left unreconciled. Not verifiable in-session: the two videos never loaded in the automated browser, but the pre-existing Product slide video shows the identical symptom, so it's the browser context, not the wiring.
- **2026-09-02** — Built the **Observability feature-intro slide** (`ObservabilityIntroSlide.tsx`), Figma `7205:102560`. Desktop values verified per-node (eyebrow gap-16 / 40px ghost-number crop band / 48-56 Season Mix in `border-frame` / 28-40 label with pt-8; headline 56/64/-0.56 with py-40; supporting line 16/28/+0.08 with pb-40; 32 between all three blocks, summing to the frame's own 416px). Three things worth recording. (1) **No mobile Figma frame exists for this slide** — the Council intro has one (`7317:12157`), this doesn't. Rather than invent proportions, the mobile treatment is CouncilIntroSlide's Figma-derived one applied unchanged (gap-56 / px-32 / py-80 container, 28px crop band, 32/40 headline, 18/30 supporting line): two sibling feature intros the reader meets in sequence shouldn't diverge on mobile on the strength of a guess. Re-verify if a frame lands. (2) **Both the headline and the supporting line are composed as explicit line breaks in Figma**, not width-constrained text — reproduced as `hidden md:block` `<br>`s so desktop matches the composition exactly while mobile reflows, the same technique CouncilIntroSlide already uses for its headline. (3) `GridDepthLayer` reuses CouncilIntroSlide's 24/40px cell sizes rather than the 44.44px tiling this frame actually draws — deliberate, for the same sibling-consistency reason. Surface is `chapter-intro-invert`, confirmed rather than assumed: this frame and the Council intro frame are both drawn on the dark surface with the header's *sun* toggle, i.e. the same inverted state, not a one-off dark slide.
- **2026-09-02** — `SegmentedRail.tsx`'s `onNavigate` changed to report a chapter **ID** instead of a chapter **index**, with `fastrouter-slides/page.tsx` resolving that ID to the chapter's FIRST slide (`navigateToChapter`). The rail had been handing its own index straight to the page's slide-index navigation — correct only while every chapter was exactly one slide, so chapter N *was* slide N. That silently stopped being true when the Council chapter grew to eight slides; adding the Observability tick was what would have exposed it (clicking Observability, chapter 5, would have scrolled to slide 5 — `council-brief`). Verified by instrumenting `scrollTo`: the four built ticks now request slides 0 / 3 / 4 / 12. Also added `observability` to `BUILT_CHAPTER_IDS` so the tick is live.
- **2026-09-02** — Built the **Rejected vs shipped slide** (`RejectedVsShippedSlide.tsx`), Figma desktop `7391:26319` / mobile `7391:27141`, registered as the Council chapter's last slide. Kept as its own component rather than sharing the vertical route's `fastrouter/RejectedVsShipped.tsx` despite identical content: the slide adds a headline and supporting paragraph the vertical version has no equivalent of, puts the [REJECTED]/[SHIPPED] tag INLINE after the column title instead of stacked above it, drops the dividers between reason rows, and sets each screenshot BESIDE its reasons on desktop (371/225 in a 612 column) rather than above them. Four things worth recording. (1) **Only the "-body" PNG is used, no chrome image** — those files already contain the app's own top bar (verified by opening one), and Figma agrees: each card's separate 546x80 chrome image is HIDDEN in both frames, unlike the vertical route which stacks chrome over body. (2) **The swapped-filename landmine documented in the vertical component is real and still unfixed** — `fr-browser-chrome-shipped-council-body.png` shows the REJECTED Playground UI and `fr-browser-chrome-rejected-pg-body.png` shows the SHIPPED Council UI. Re-verified by opening the file rather than taken on trust, and wired here by what each image CONTAINS. Two call sites now depend on the inversion; renaming the source files would fix both at once. (3) **Figma conflict, normalized:** the supporting paragraph is bound to `text-muted` on desktop and `text-primary` on mobile — took `text-muted` for both (role-correct for a supporting line, and the desktop frame is where the composition was designed). Same class of disagreement as the Reframe slide's struck-through line. (4) The reason-row body is bound to Brand/Dark (#1E2B14) in BOTH frames — the Twitter/X colour already identified as a copy-paste artifact in the vertical route's version of this section; uses `text-primary` here for the same reason. Figma's "blur" layer (a band over each card's bottom edge) is reproduced as a MASK on the card — see the follow-up entry below. Mobile is a real frame, not a guess: flat gap-24 header stack, 32/40 headline, 14/20 column titles, columns stacked 60 apart, card above its reasons at gap-8.
- **2026-09-02** — Follow-up round on the slide above, all four flagged directly. (a) **Reason-row thumb icons 24px → 16px.** (b) **Slide re-sequenced to sit right after the Reframe slide** (deck index 12 → 8), ahead of the four decisions rather than after them — the two parallel arrays that needed no edit (`SLIDE_CHAPTER_IDS`, `SLIDE_RAIL_MODE`) are uniform `council`/`follow` across that whole span. (c) **Card fade rebuilt from an overlay gradient to a `mask-image`** ("should be like the product and tweet page scenarios, fading into the background"): the first pass painted an opaque `transparent → bg-primary` band INSIDE a card that carries a border and an 8px radius, so the bottom border and its two corners stayed crisply drawn behind the band — a bordered box with a gradient inside it, not something dissolving. Masking the card takes the border and corners with it. Same mechanic and ramp (`black 72% → transparent 97%`) as the Conceptual Grounding tweet fix earlier the same day; worth noting the Product recording gets the right read from a plain overlay only because its container has no bottom border to give it away, which is why copying that approach here didn't transfer. (d) **Gap between the supporting paragraph and the two comparison blocks 40 → 56px on desktop** (mobile's 24 unchanged); 56 isn't on the spacing scale so it's an arbitrary value, not a named `gap-56px` (which silently collapses to 0). Measured live afterwards: 56px gap, 16px icons.

## Undated (flagged in code/CLAUDE.md, no specific date recorded)

- decision-title text style renders in Google Sans Flex though Figma still binds it to (stale) DM Sans — same stale binding also affects CockpitPlaceholder.tsx's placeholder caption and CouncilIntro.tsx's "CONCEPTUAL GROUNDING" card title. CLAUDE.md notes Figma needs updating, not the code.
- "ONE HONEST FAILURE" section padding normalized to 60px top/bottom in code vs. Figma's 64px, to match every other section's rhythm; its internal label/paragraph/pull-quote gaps rounded to 10px/20px vs. Figma's off-grid 10.7px/18.5px, since that section was built off-system in Figma.
- Council intro stack gaps set to 64px in code vs. Figma's 56px, to match the established 32/64 doubling rhythm used elsewhere.
- CouncilIntro.tsx dark card border uses the (now-confirmed, see 2026-08-20 dated entry) translucent border-default token instead of Figma's solid #D9D9D9 — a deliberate token-over-literal choice for this one card, not a sign-off gap.
- EvaluationsFeature.tsx: a 3rd hidden duplicate "key decision" in Figma's block 4 was skipped (only 2 of 3 authored decisions built) — it duplicates content already covered elsewhere, matching the pattern for other hidden Figma nodes.
- FiveDecisions.tsx "0N" numeral color uses border-frame (#D5D5D5) instead of Figma's literal #D9D9D9 (4 units off) — no exact semantic token exists for the literal value.
- All three case studies' Footer.tsx: only "Email" is a real link; LinkedIn/AI Practices/Design Taste/Store render as inert text — no URLs are specified anywhere in Figma or content docs, and links can't be fabricated.
- Hero.tsx (fastrouter): divider border uses border-default instead of Figma's bound color/text-on-dark (#D9D9D9), which reads as a dark-mode style mistakenly applied to a light-mode stroke. Mobile side padding uses `--edge-padding` (24px), flagged PROPOSED/unconfirmed in design-tokens.json.
- MetaBar.tsx (fastrouter): section uses 0px top / 60px bottom padding, an undocumented rhythm exception measured directly off the Figma node.
- A "before/after slider" block (FR-01 vs FR-02 exploration) exists in Figma across FastRouter (ObservabilityIntro.tsx), Analytics (FeatureQueryBreakdown.tsx), and Extensions (FeatureMaps.tsx, FeaturePhotoEditor.tsx) but is skipped in all of them — hidden in Figma in every case, and no slider component exists in the codebase yet.
- RejectedVsShipped.tsx: two "-body.png" asset filenames are swapped relative to their actual content (confirmed via a dashboard URL baked into each screenshot); code wires images by actual content, not filename — flagged as a landmine the source files should be renamed to fix. Same file's reason-list body text uses text-primary (#0D0D0D) instead of Figma's bound Brand/Dark (#1E2B14), which looks like a copy-paste artifact from the tweet embed elsewhere in the file.
- Impact.tsx (fastrouter): before/after card padding rounded from Figma's literal px-29/py-25 to the established p-32px used elsewhere in the section; stat number color mapped to text-primary (#0D0D0D) instead of Figma's literal #111111 (~4-unit difference) — both treated as drift within the build's existing tolerance band.
- ProblemAndProduct.tsx (fastrouter): all divider borders use border-default, though the same Figma node inconsistently binds some to color/text-on-dark (#D9D9D9) — a mode-mismatch slip in the source file.
- ThanksForReading.tsx: "Let's talk →" wired to a real mailto: address, since Figma specifies no destination for this button and a fabricated contact page was avoided.
- analytics/page.tsx: Figma node 6379:57064 was declared canonical for Analytics content, superseding CLAUDE.md's note that `/content` docs are canonical for Analytics until told otherwise — per direct instruction once real Figma content became available.
- analytics/CockpitPlaceholder.tsx and extensions/CockpitPlaceholder.tsx: placeholder illustration text/caption are byte-for-byte identical to fastrouter/CockpitPlaceholder.tsx, including FastRouter-specific language — built verbatim since Figma's own content for these blocks appears to be an uncustomized copy-paste.
- analytics/FeatureQueryBreakdown.tsx: rejected-direction card tag still reads "FR-11" (FastRouter's own numbering), kept verbatim rather than relabeled for Analytics.
- analytics/Hero.tsx: breadcrumb's first segment renders as plain text instead of an inline logomark SVG the way FastRouter's Hero does — no Analytics logomark asset exists yet.
- analytics/ProblemAndProduct.tsx swaps the case-study-specific ProductBrowserMockup for a generic ScreenshotFrame + AssetPlaceholder at the same aspect ratio, since no Analytics product mockup asset exists yet.
- analytics/RejectedDirectionCard.tsx: the single-card "REJECTED DIRECTION" design has no exact Figma reference — FastRouter's Figma only established 2-column REJECTED/SHIPPED patterns, so this was built from engineering judgment reusing established tokens.
- extensions/FiveMoreExtensions.tsx and Impact.tsx: both sections' intro paragraphs are word-for-word identical, duplicating the same job — built verbatim since the source Figma file itself has two sections doing the same job rather than differentiating or de-duplicating them.
- extensions/OneHonestFailure.tsx dropped the pull-quote sub-block that fastrouter/HonestFailure.tsx's structure otherwise includes, since only a label+paragraph were captured in this section's extracted Figma content — a quote wasn't invented to fill the slot.
- shared/TempHeader.tsx: a temporary sticky preview header with a dark-mode toggle exists across the whole site — no header exists in FastRouter's Figma frames at all; scaffolding to be removed once dark mode has a real home.
- shared/ShareButton.tsx implements Web Share API + clipboard-copy fallback behavior that Figma never specified — Figma's button only says "Share this case Study →" with no destination or share target defined.
- src/app/page.tsx: FastRouter's project year is inconsistent across source files (analytics/ReadNext.tsx says 2024, extensions/ReadNext.tsx says 2026) — the index page went with 2026 to match MetaBar.tsx's timeline and the footer copyright year; analytics/ReadNext.tsx still needs reconciling.
- practice/PracticeFeed.tsx: CSS-columns masonry can't preserve strict left-to-right chronological post order (fills top-to-bottom per column) — accepted trade-off to avoid a JS masonry library, per spec.
- globals.css `--edge-padding` (24px) mobile spacing value is marked PROPOSED — no mobile Figma frame exists below ~1060px viewport to source a real value from.
- SectionRail.tsx's appearance trigger was originally tied to the "intro" section's own exit point, changed to a plain scroll-distance threshold — the intro section's actual height pushed the appearance point much further down than intended, per direct feedback.
